package id.itborneo.wisatasamarinda.utils

import android.view.View

object SpinKitUtils {

    fun show(spinKitView: View) {
        spinKitView.visibility = View.VISIBLE
    }

    fun hide(spinKitView: View) {
        spinKitView.visibility = View.GONE
    }

}